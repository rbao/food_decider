.span6.offset3
  %h1 Start Deciding
  = form_for @deciding do |f|
    %fieldset
      = render 'shared/error_messages', object: f.object
      = render 'fields', f: f
      .btn-group
        = f.submit 'Start', class: 'btn btn-primary'
